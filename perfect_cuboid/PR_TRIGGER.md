# Heegner computation trigger

This draft branch runs three independent exact rank-one generator searches for the `(p,q)=(43,71)` perfect-cuboid frontier:

1. PARI `ellheegner`;
2. deeper mwrank 2-descent with increasing second-descent limits;
3. a fixed Heegner discriminant `D=-50231` of class number `337`.
