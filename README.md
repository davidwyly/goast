# GOAST scratch computations

Temporary public Sage/GP scratchpad for exact arithmetic research runs.

Active target: recover the missing rank-one generator for the perfect-cuboid frontier `(p,q)=(43,71)` by PARI `ellheegner`.
